
# Postgres:
#installed postgres 9.0 on mac w/ macports
#Following Aron Bedra's Heroku + postgresql note:
#https://devcenter.heroku.com/articles/clojure-web-application

# Set up a server
initdb pg
postgres -D pg &
createdb hits
export DATABASE_URL=postgresql://localhost:5432/hits

# ... test w/ repl:
lein repl
# All his examples worked for me

# Postgres on Ubuntu:
# -- Local
sudo apt-get install postgresql  libpq-dev
createdb hits
psql hits # Open this database in a terminal

# -- Set up a server
# http://linuxpoison.blogspot.com/2012/01/how-to-install-configure-postgresql.html
# Also need to add which hosts we trust
# This is in the file /etc/postgresql/9.1/main/pg_hba.conf
# http://www.cyberciti.biz/faq/postgresql-remote-access-or-connection/

### OLD Django stuff, disregard:


# https://devcenter.heroku.com/articles/django
export PNAME=hits
mkdir $PNAME && cd $PNAME
virtualenv venv
source venv/bin/activate
easy_install pip
sudo apt-get install libpq-dev python-dev
pip install Django dj-database-url psycopg2
django-admin.py startproject $PNAME
cd $PNAME

# Start test server:
python manage.py runserver 0.0.0.0:8000

# ... then browse to Web server location http://<ip>:8000 where <ip>
#  is the public IP address.  Make sure port 8000 is open if not
#  browsing locally.

